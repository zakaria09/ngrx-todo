import { AutoselectDirective } from './autoselect.directive';

describe('AutofocusDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoselectDirective();
    expect(directive).toBeTruthy();
  });
});
